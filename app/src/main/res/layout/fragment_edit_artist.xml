<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingBottom="12dp" >

    <TextView
        android:id="@+id/editArtistInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="8dp"
        android:layout_marginVertical="8dp"
        android:text="@string/artist_information"
        android:textSize="14sp"
        android:textColor="@color/accent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/editArtistNameHeading"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="8dp"
        android:layout_marginVertical="4dp"
        android:text="@string/artist_name"
        android:textSize="12sp"
        app:layout_constraintTop_toBottomOf="@id/editArtistInfo" />

    <EditText
        android:id="@+id/editArtistName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="8dp"
        android:layout_marginVertical="4dp"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/editArtistNameHeading"
        android:inputType="text"
        android:maxLength="100"
        android:hint="@string/artist"
        android:importantForAutofill="no" />
</androidx.constraintlayout.widget.ConstraintLayout>